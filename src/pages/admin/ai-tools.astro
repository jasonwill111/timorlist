---
---
export const prerender = false;
import AdminLayout from '@layouts/AdminLayout.astro';
import { actions } from 'astro:actions';
import { Textarea } from '@/components/fulldev/textarea';
import { Input } from '@/components/fulldev/input';
import { Select } from '@/components/fulldev/select';
import { Button } from '@/components/fulldev/button';
import { Badge } from '@/components/fulldev/badge';
import { Label } from '@/components/fulldev/label';
import Progress from '@/components/starwind/progress/Progress.astro';
import AIToolsIsland from '@/components/islands/admin/AIToolsIsland.astro';

<AdminLayout title="AI Tools">
  <div class="container py-6 px-4">
    <!-- Page Header -->
    <div class="mb-6">
      <h1 class="text-3xl font-bold leading-tight">AI Tools</h1>
      <p class="text-sm text-muted-foreground">Generate listings, products, blog posts, and landing pages with AI</p>
    </div>

    <!-- Tab Navigation -->
    <div class="mb-6 border-b border-border">
      <nav class="-mb-px flex space-x-6">
        <Button id="tab-listing" variant="ghost" class="tab-btn py-3 px-1 border-b-2 font-medium text-sm border-primary text-primary" data-tab="listing">
          Listing Generator
        </Button>
        <Button id="tab-sku" variant="ghost" class="tab-btn py-3 px-1 border-b-2 font-medium text-sm border-transparent text-muted-foreground hover:text-foreground" data-tab="sku">
          Product/SKU Generator
        </Button>
        <Button id="tab-blog" variant="ghost" class="tab-btn py-3 px-1 border-b-2 font-medium text-sm border-transparent text-muted-foreground hover:text-foreground" data-tab="blog">
          Blog Article Generator
        </Button>
        <Button id="tab-landing" variant="ghost" class="tab-btn py-3 px-1 border-b-2 font-medium text-sm border-transparent text-muted-foreground hover:text-foreground" data-tab="landing">
          Landing Page Generator
        </Button>
      </nav>
    </div>

    <!-- Listing Generator -->
    <div id="listing-section" class="tab-content">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <div class="bg-card border rounded-lg p-6">
          <h2 class="text-lg font-semibold leading-snug mb-4">Create New Listing</h2>
          <form id="listing-form" class="space-y-4">
            <div>
              <Label for="listing-title">Business Name *</Label>
              <Input type="text" id="listing-title" required class="w-full px-3 py-2 border rounded-md text-sm" placeholder="e.g. Timor Coffee Shop" />
            </div>
            <div>
              <Label for="listing-entity-type">Entity Type *</Label>
              <Select id="listing-entity-type" required>
                <option value="business">Business</option>
                <option value="nonprofit">Non-Profit</option>
              </Select>
            </div>
            <div>
              <Label for="listing-category">Category</Label>
              <Select id="listing-category">
                <option value="">Select category...</option>
                <option value="cat-1">Restaurants & Cafes</option>
                <option value="cat-2">Hotels & Accommodation</option>
                <option value="cat-3">Shopping</option>
                <option value="cat-4">Health & Beauty</option>
                <option value="cat-5">Automotive</option>
                <option value="cat-6">Professional Services</option>
                <option value="cat-7">Education</option>
                <option value="cat-8">Entertainment</option>
                <option value="cat-9">Travel & Tours</option>
                <option value="cat-10">Construction</option>
              </Select>
            </div>
            <div class="grid grid-cols-2 gap-4">
              <div>
                <Label for="listing-contact">Contact Name</Label>
              <Input type="text" id="listing-contact" class="w-full px-3 py-2 border rounded-md text-sm" placeholder="Contact person" />
              </div>
              <div>
                <Label for="listing-phone">Phone</Label>
              <Input type="text" id="listing-phone" class="w-full px-3 py-2 border rounded-md text-sm" placeholder="77000000" />
              </div>
            </div>
            <div>
              <Label for="listing-email">Email</Label>
              <Input type="email" id="listing-email" class="w-full px-3 py-2 border rounded-md text-sm" placeholder="info@example.tl" />
            </div>
            <div>
              <Label for="listing-address">Address</Label>
              <Input type="text" id="listing-address" class="w-full px-3 py-2 border rounded-md text-sm" placeholder="Avenida Principal, Dili" />
            </div>
            <div>
              <Label for="listing-about">About Us</Label>
              <Textarea id="listing-about" rows="3" class="w-full px-3 py-2 border rounded-md text-sm" placeholder="Tell customers about your business..." />
            </div>
            <div>
              <Label for="listing-tags">Tags (comma-separated)</Label>
              <Input type="text" id="listing-tags" class="w-full px-3 py-2 border rounded-md text-sm" placeholder="coffee, wifi, local" />
            </div>
            <div>
              <Button type="submit" variant="default" class="w-full">
                Generate Listing
              </Button>
            </div>
          </form>
        </div>

        <!-- Preview Panel -->
        <div class="bg-card border rounded-lg p-6">
          <div class="flex items-center justify-between mb-4">
            <h2 class="text-lg font-semibold leading-snug">Generated Preview</h2>
            <span id="listing-status" class="px-2 py-1 text-xs rounded-full bg-muted">Ready</span>
          </div>
          <div id="listing-loading" class="hidden py-12 space-y-4">
            <Progress variant="primary" />
            <p class="text-center text-muted-foreground">Generating listing...</p>
          </div>
          <div id="listing-preview" class="hidden space-y-4">
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Title</span>
              <p id="preview-listing-title" class="font-medium"></p>
            </div>
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Entity Type</span>
              <p id="preview-listing-type" class="font-medium"></p>
            </div>
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Contact</span>
              <p id="preview-listing-contact" class="text-sm"></p>
            </div>
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Address</span>
              <p id="preview-listing-address" class="text-sm"></p>
            </div>
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Tags</span>
              <p id="preview-listing-tags" class="text-sm"></p>
            </div>
            <div class="flex gap-2 pt-4">
              <Button id="listing-preview-btn" type="button" variant="default" class="flex-1">
                Preview
              </Button>
              <Button id="listing-save-btn" type="button" variant="default" class="flex-1">
                Save Listing
              </Button>
              <Button id="listing-copy-btn" type="button" variant="outline" class="flex-1">
                Copy
              </Button>
            </div>
          </div>
          <div id="listing-empty" class="py-12 text-center text-muted-foreground">
            <svg class="w-16 h-16 mx-auto mb-4 opacity-50" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
            </svg>
            <p>Fill the form and click Generate</p>
          </div>
        </div>
      </div>
    </div>

    <!-- SKU Generator -->
    <div id="sku-section" class="tab-content hidden">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <div class="bg-card border rounded-lg p-6">
          <h2 class="text-lg font-semibold leading-snug mb-4">Create Product/Service</h2>
          <form id="sku-form" class="space-y-4">
            <div>
              <Label for="sku-title">Product/Service Title *</Label>
              <Input type="text" id="sku-title" required class="w-full px-3 py-2 border rounded-md text-sm" placeholder="e.g. Wireless Headphones" />
            </div>
            <div>
              <Label for="sku-type">Service Type *</Label>
              <Select id="sku-type" required>
                <option value="product">Product</option>
                <option value="service">Service</option>
                <option value="rental">Rental</option>
                <option value="food">Food</option>
                <option value="accommodation">Accommodation</option>
                <option value="project">Project</option>
              </Select>
            </div>
            <div>
              <Label for="landing-description">Description</Label>
              <Textarea id="sku-description" rows="3" class="w-full px-3 py-2 border rounded-md text-sm" placeholder="Product description..." />
            </div>
            <div>
              <Label for="sku-price-label">Price Label</Label>
              <Input type="text" id="sku-price-label" class="w-full px-3 py-2 border rounded-md text-sm" placeholder="e.g. Hourly Rate, Per Day" />
            </div>
            <div class="grid grid-cols-2 gap-4">
              <div>
                <Label for="sku-price-value">Price Value</Label>
                <Input type="text" id="sku-price-value" class="w-full px-3 py-2 border rounded-md text-sm" placeholder="25.00" />
              </div>
              <div>
                <Label for="sku-price-unit">Unit</Label>
                <Input type="text" id="sku-price-unit" class="w-full px-3 py-2 border rounded-md text-sm" placeholder="/hour, /day" />
              </div>
            </div>
            <div>
              <Button type="submit" variant="default" class="w-full">
                Generate SKU
              </Button>
            </div>
          </form>
        </div>
        <div class="bg-card border rounded-lg p-6">
          <div class="flex items-center justify-between mb-4">
            <h2 class="text-lg font-semibold leading-snug">Generated Preview</h2>
            <span id="sku-status" class="px-2 py-1 text-xs rounded-full bg-muted">Ready</span>
          </div>
          <div id="sku-loading" class="hidden py-12 space-y-4">
            <Progress variant="primary" />
            <p class="text-center text-muted-foreground">Generating SKU...</p>
          </div>
          <div id="sku-preview" class="hidden space-y-4">
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Title</span>
              <p id="preview-sku-title" class="font-medium"></p>
            </div>
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Service Type</span>
              <p id="preview-sku-type" class="font-medium"></p>
            </div>
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Price</span>
              <p id="preview-sku-price" class="font-medium"></p>
            </div>
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Description</span>
              <p id="preview-sku-desc" class="text-sm"></p>
            </div>
            <div class="flex gap-2 pt-4">
              <Button id="sku-save-btn" type="button" variant="default" class="flex-1">
                Save SKU
              </Button>
              <Button id="sku-copy-btn" type="button" variant="outline" class="flex-1">
                Copy JSON
              </Button>
            </div>
          </div>
          <div id="sku-empty" class="py-12 text-center text-muted-foreground">
            <p>Fill the form and click Generate</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Blog Generator -->
    <div id="blog-section" class="tab-content hidden">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <div class="bg-card border rounded-lg p-6">
          <h2 class="text-lg font-semibold leading-snug mb-4">Generate Blog Article</h2>
          <form id="blog-form" class="space-y-4">
            <div>
              <Label for="blog-topic">Topic *</Label>
              <Input type="text" id="blog-topic" required class="w-full px-3 py-2 border rounded-md text-sm" placeholder="e.g. Best Coffee Shops in Dili" />
            </div>
            <div class="grid grid-cols-2 gap-4">
              <div>
                <Label for="blog-type">Article Type</Label>
                <Select id="blog-type">
                  <option value="business-introduction">Business Introduction</option>
                  <option value="local-highlight">Local Highlight</option>
                  <option value="event-announcement">Event Announcement</option>
                  <option value="how-to-guide">How-To Guide</option>
                  <option value="industry-news">Industry News</option>
                  <option value="community-story">Community Story</option>
                </Select>
              </div>
              <div>
                <Label for="blog-length">Length</Label>
                <Select id="blog-length">
                  <option value="short">Short (~300 words)</option>
                  <option value="medium" selected>Medium (~600 words)</option>
                  <option value="long">Long (~1200 words)</option>
                </Select>
              </div>
            </div>
            <div>
              <Label for="blog-prompt">Additional Requirements</Label>
              <Textarea id="blog-prompt" rows="4" class="w-full px-3 py-2 border rounded-md text-sm" placeholder="Key points to cover, specific businesses to mention..." />
            </div>
            <div>
              <Button type="submit" variant="default" class="w-full">
                Generate Article
              </Button>
            </div>
          </form>
        </div>
        <!-- Preview Panel -->
        <div class="bg-card border rounded-lg p-6">
          <div class="flex items-center justify-between mb-4">
            <h2 class="text-lg font-semibold leading-snug">Generated Article</h2>
            <span id="blog-status" class="px-2 py-1 text-xs rounded-full bg-muted">Ready</span>
          </div>
          <div id="blog-loading" class="hidden py-12 space-y-4">
            <Progress variant="primary" />
            <p class="text-center text-muted-foreground">Generating article...</p>
          </div>
          <div id="blog-preview" class="hidden space-y-4">
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Title</span>
              <p id="preview-blog-title" class="font-medium"></p>
            </div>
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Excerpt</span>
              <p id="preview-blog-excerpt" class="text-sm"></p>
            </div>
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Content</span>
              <div id="preview-blog-content" class="text-sm max-h-64 overflow-y-auto"></div>
            </div>
            <div class="flex gap-2 pt-4">
              <Button id="blog-preview-btn" type="button" variant="default" class="flex-1">
                Preview
              </Button>
              <Button id="blog-save-btn" type="button" variant="default" class="flex-1">
                Save Draft
              </Button>
              <Button id="blog-copy-btn" type="button" variant="outline" class="flex-1">
                Copy
              </Button>
            </div>
          </div>
          <div id="blog-empty" class="py-12 text-center text-muted-foreground">
            <p>Fill the form and click Generate</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Landing Page Generator -->
    <div id="landing-section" class="tab-content hidden">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <div class="bg-card border rounded-lg p-6">
          <h2 class="text-lg font-semibold leading-snug mb-4">Create Landing Page</h2>
          <form id="landing-form" class="space-y-4">
            <div>
              <Label for="landing-title">Page Title *</Label>
              <Input type="text" id="landing-title" required class="w-full px-3 py-2 border rounded-md text-sm" placeholder="e.g. Summer Sale 2026" />
            </div>
            <div>
              <Label for="landing-type">Page Type</Label>
              <Select id="landing-type">
                <option value="promotion">Promotion / Sale</option>
                <option value="product-showcase">Product Showcase</option>
                <option value="event">Event</option>
                <option value="seasonal">Seasonal Campaign</option>
              </Select>
            </div>
            <div>
              <Label for="landing-description">Description</Label>
              <Textarea id="landing-description" rows="2" class="w-full px-3 py-2 border rounded-md text-sm" placeholder="Brief description..." />
            </div>
            <div>
              <Label for="landing-prompt">Requirements for AI</Label>
              <Textarea id="landing-prompt" rows="4" class="w-full px-3 py-2 border rounded-md text-sm" placeholder="Key message, target audience, specific products..." />
            </div>
            <div>
              <Button type="submit" variant="default" class="w-full">
                Generate Landing Page
              </Button>
            </div>
          </form>
        </div>
        <!-- Preview Panel -->
        <div class="bg-card border rounded-lg p-6">
          <div class="flex items-center justify-between mb-4">
            <h2 class="text-lg font-semibold leading-snug">Generated Page</h2>
            <span id="landing-status" class="px-2 py-1 text-xs rounded-full bg-muted">Ready</span>
          </div>
          <div id="landing-loading" class="hidden py-12 space-y-4">
            <Progress variant="primary" />
            <p class="text-center text-muted-foreground">Generating landing page...</p>
          </div>
          <div id="landing-preview" class="hidden space-y-4">
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Hero Title</span>
              <p id="preview-landing-hero-title" class="font-medium"></p>
            </div>
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Hero Subtitle</span>
              <p id="preview-landing-hero-subtitle" class="text-sm"></p>
            </div>
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Description</span>
              <p id="preview-landing-desc" class="text-sm"></p>
            </div>
            <div class="p-4 bg-muted rounded-lg">
              <span class="text-xs text-muted-foreground">Features</span>
              <ul id="preview-landing-features" class="text-sm list-disc pl-4"></ul>
            </div>
            <div class="flex gap-2 pt-4">
              <Button id="landing-preview-btn" type="button" variant="default" class="flex-1">
                Preview
              </Button>
              <Button id="landing-save-btn" type="button" variant="default" class="flex-1">
                Save Page
              </Button>
              <Button id="landing-copy-btn" type="button" variant="outline" class="flex-1">
                Copy
              </Button>
            </div>
          </div>
          <div id="landing-empty" class="py-12 text-center text-muted-foreground">
            <p>Fill the form and click Generate</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</AdminLayout>

<AIToolsIsland />
